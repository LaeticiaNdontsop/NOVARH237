Django>=5.0,<5.1
psycopg2-binary>=2.9
dj-database-url>=2.1
python-dotenv>=1.0
Pillow>=10.0
django-widget-tweaks>=1.5

# A ajouter au Jour 4 (analyse / IA) -- deja utilise pour la prediction d'attrition
# IMPORTANT : scikit-learn est fige a la version utilisee pour ENTRAINER le modele
# (ml/modele_rh.pkl, scaler.pkl, encodeurs.pkl). Utiliser une autre version peut
# degrader silencieusement la qualite des predictions.
pandas>=2.2
scikit-learn==1.7.2
joblib>=1.3
# google-generativeai est deprecie par Google (plus de correctifs de securite) :
# on utilise le nouveau SDK unifie google-genai (import `from google import genai`).
google-genai>=1.0
